var expect = require('chai').expect;

describe('Passport authorization', function() {

  it('should return 401 when accessing /dashboard not logged in', function() {
    
  });

  it('should be able to register', function() {
    
  });

  it('should login after registering automatically', function() {
    
  });
  

  it('should be able to login', function() {
    
  });
  
  it('should access public static files without auth', function() {
    
  });
  
  it('should be required to login when trying to access base angular url', function() {
    
  });

  it('should be able to access home, login, register without auth', function() {
    
  });
  
  
  
   

  
});
